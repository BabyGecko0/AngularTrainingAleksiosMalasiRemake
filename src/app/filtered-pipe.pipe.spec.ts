import { FilteredPipePipe } from './filtered-pipe.pipe';

describe('FilteredPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilteredPipePipe();
    expect(pipe).toBeTruthy();
  });
});
